# Secure Dockerfile with fixes applied
FROM python:3.12-slim
COPY app/ /app/
WORKDIR /app

RUN pip install flask requests && \
    useradd -m -u 1000 appuser && \
    chown -R appuser:appuser /app

USER appuser

EXPOSE 5000
CMD ["python", "main.py"]
